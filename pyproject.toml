[project]
name = "dbt-course"
version = "1.0.0"
description = "Environment for the dbt for Data Analysts & Engineers course"
requires-python = ">=3.11"
dependencies = [
    "dbt-core>=1.9",
    "dbt-duckdb>=1.9",
    "dbt-bigquery>=1.9",
    "duckdb>=1.1",
]
