# Module 1 — Models and dependencies

| # | Lesson | Pain |
| --- | --- | --- |
| 1.1 | From SELECT to model: transformation as versioned code | "my CREATE VIEW scripts aren't versioned or repeatable" |
| 1.2 | Dependencies and build order: `ref`, the DAG, compiled SQL | "how do I chain models without hardcoding names and guessing run order?" |
| 1.3 | The materialization decision: view vs table vs incremental | "some models are slow, some need to be fresh, which do I persist?" |

<!-- exercises land here during the build -->
