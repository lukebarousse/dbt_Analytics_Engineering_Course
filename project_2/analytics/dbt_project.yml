name: 'analytics'
profile: 'databricks'

models:
  analytics:
    +persist_docs:
      relation: true
      columns: true
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
    utilities:
      +materialized: table
      +schema: marts

seeds:
  analytics:
    +schema: seeds
