name: 'analytics'
profile: 'databricks'

models:
  analytics:
    +persist_docs:
      relation: true
      columns: true
    staging:
      +materialized: view
      +schema: silver
    intermediate:
      +materialized: view
      +schema: silver
    marts:
      +materialized: table
      +schema: gold
    utilities:
      +materialized: table
      +schema: gold

seeds:
  analytics:
    +schema: silver
