# models/sources.yml — declares things dbt didn't build (sources ONLY)
sources:
  - name: raw
    tables:
      - name: job_postings
        description: One row per job-posting scrape, raw from the pipeline.
        meta:
          external_location: "read_parquet('../data/raw/raw_job_postings_*.parquet')"